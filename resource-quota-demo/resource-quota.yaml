apiVersion: v1
kind: ResourceQuota
metadata:
  name: workload-quota
  namespace: resource-quota-demo
spec:
  hard:
    pods: "5"
    requests.cpu: "500m"
    requests.memory: "256Mi"
    limits.cpu: "1"
    limits.memory: "512Mi"
